pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven {
            url "https://linphone.org/maven_repository/"
        }
        google()
        mavenCentral()
    }
}
rootProject.name = "wanted"
include ':app'
